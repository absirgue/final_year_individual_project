# Iterates over a series of algorithms and parameter settings, creates Cluster classes
# calculates the overall entropy + the number of significant sized clusters
# (where you have investment and junk ratings)